<div class="game-main-container">

  <%= render "game_header" %>

  <div class="game-container" data-url="<%= game_plays_url(@game) %>">
    <% if ["Hiragana 1", "Katakana 1"].include?(@game.name) %>
    <%= render "kana_game" %>
    <% else %>
    <%= render "icon_game" %>
    <% end %>
  </div>
</div>

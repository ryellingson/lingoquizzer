<%= form_for (Post.new), remote: true do |f| %>

<%= f.text_field :language_id, value: @language.id, hidden: true %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :content %>
<%= f.text_area :content %>

<%= f.submit "Create post", class: "post-button border-glow" %>

<% end %>

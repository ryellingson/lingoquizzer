<!DOCTYPE html>
<html>
  <%= render "shared/head" %>
  <body style="background: rgb(118,150,120);">
    <div class="container mx-auto px-4 h-screen" style="background: rgb(118,150,120);">
      <div class="max-w-xl mx-auto border border-gray-600 rounded p-5 bg-white h-screen overflow-scroll">
        <%= render 'layouts/alerts' %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>

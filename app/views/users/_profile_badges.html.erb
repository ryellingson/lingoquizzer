<div class="plays-cards">
 <!--  <%= @user.badges %> -->
  <% @user.badges_to_display.each do |badge| %>
    <%= image_tag badge.custom_fields[:image_path], class: "plays-card", data: {id: badge.id} %>
  <% end %>
</div>
